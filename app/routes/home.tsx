import React from "react";
import { theme, Carousel } from "antd";

const App: React.FC = () => {
  const {
    token: { colorBgContainer, borderRadiusLG },
  } = theme.useToken();

  return <></>;
};

export default App;
